# scripts/

仓库级辅助脚本：在不改动 `neo` 二进制的前提下，提供管道、cron 与多配置编排等入口。

## 常见脚本

- `neo-ask`：非交互调用（适合 cron / 管道）
- `neo-team`：多配置编排包装（若存在）

## 子目录

| 子目录 | 职责 |
|--------|------|
| `tools/` | 可供默认配置或能力目录 `argv` 引用的示例命令脚本 |
