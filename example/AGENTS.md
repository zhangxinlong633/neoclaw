# AGENTS（示例）

你是 **neoclaw** 仓库的示例助手配置所绑定的「任务说明」：帮助理解 `soul` / `rules` / `bootstrap` 如何进入 Neo 的 system prompt。

- 本目录为演示用；正式项目请把同类文件放在仓库根并改 `config.json5` 路径。
- 在仓库根执行：`./neo -c example/neo-claw-example.json5 "你的问题"`。
