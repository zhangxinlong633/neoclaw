{
  name: "unix_nc",
  description: "TCP/UDP swiss army knife.",
  when: ["Need raw sockets"],
  when_not: ["Dangerous", "Not in default enabled.json5; enable only after Policy review"],
  tags: ["unix", "net", "dangerous"],
  outcome: "Usage: nc -vz host 80 | Sample: ",
  sample_usage: "nc -vz host 80",
  sample_output: "",
  argv: ["./scripts/tools/unix-exec.sh", "/usr/bin/nc"],
  timeout_sec: 15,
  max_output_bytes: 32768,
  pass_args: "env",
  parameters: {"type": "object", "properties": {"argv": {"type": "array", "items": {"type": "string"}, "description": "Extra argv after the binary. Relative paths only; no ..; simple -x/--long flags."}}},
}
