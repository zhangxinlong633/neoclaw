{
  name: "unix_install",
  description: "Copy files with mode.",
  when: ["Need install(1)"],
  when_not: ["Keep disabled", "Not in default enabled.json5; enable only after Policy review"],
  tags: ["unix", "fs", "dangerous"],
  outcome: "Usage: install -m 755 a b | Sample: ",
  sample_usage: "install -m 755 a b",
  sample_output: "",
  argv: ["./scripts/tools/unix-exec.sh", "/usr/bin/install"],
  timeout_sec: 15,
  max_output_bytes: 32768,
  pass_args: "env",
  parameters: {"type": "object", "properties": {"argv": {"type": "array", "items": {"type": "string"}, "description": "Extra argv after the binary. Relative paths only; no ..; simple -x/--long flags."}}},
}
